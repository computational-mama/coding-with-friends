<nav class="flex items-center justify-between flex-wrap px-5 py-3 border-b">
  <a href="/" class="font-mono font-black text-xl tracking-tight">Home</a>

  {% set navPages = collections.all | eleventyNavigation %}
  <ul>
    {% for entry in navPages %}
      <li class="inline">
        <a
         href="{{ entry.url }}"
         class="uppercase text-sm py-1 px-2 rounded mr-4 inline-block
         {{'bg-white text-black' if entry.url in page.url
         else 'text-gray-700 hover:text-gray-900'}}">
         {{ entry.title }}
       </a>
      </li>
    {% endfor %}
        <li class="inline">
        <a
         href="https://twitch.tv/computational_mama" target="_blank"
         class="uppercase text-sm py-1 px-2 rounded mr-4 inline-block">
         Twitch
       </a>
      </li>
 </ul>
</nav>
